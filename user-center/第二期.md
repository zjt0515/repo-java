## Controller 控制层封装请求

`@RestController`

一套接口的规范

### 封装请求对象，专门用于接收请求

```
@RequestBody
//让mvc框架知道，将前端传来的json参数和这个对象做一个关联
```

controller层倾向于对请求参数本身的校验，不涉及业务逻辑本身

service层是对业务逻辑的校验（有可能呗controller之外的类调用

## 用户管理接口

必须鉴权！

1. 根据用户名查询用户
2. 删除用户
