## 依赖

### lombok

`@Slf4j`为对应的类打上注解，就可以使用log输出日志

```

```

## 插件

### Auto filling...

alt+enter自动插入形参    

> [!CAUTION]
>
> 改动字段后，建议全局搜索，替换？
>
> 尤其要关注一下几个User、UserMapper、UserService...........

### Generateor All Getter And Setter

使用`.getall()`打印所有get方法



### codegen自动生成前端请求代码

`npm install openapi-typescript-codegen --save dev`

`openapi --input http://localhost:8121/api/v2/api-docs --output ./generated --client axios`

`npx openapi-typescript-codegen --input http://localhost:8121/api/v2/api-docs --output ./generated --client axios`

[一键生成请求方法的工具 —— OpenAPI Typescript Codegen_openapi-typescript-codegen-CSDN博客](https://blog.csdn.net/qq_39921135/article/details/136282435#:~:text=是一个 Node.js,工具，用于执行安装在项目依赖中的可执行文件。 它的作用是在不全局安装的情况下，临时运行项目依赖中的命令。 执行完成后，在项目根目录中会出现下图目录，可以看见，它直接给我们生成了向后端发请求的函数，当需要使用时，只需要直接调用就好了。)

## Service业务层

## Controller接口层

controller主要是调用userService中的方法

### 请求类型选择

> [!warning]
>
> 登录和注销不建议用get，一般用post或者其他。。
> `@PostMapping("/login")`
> `@PostMapping("/register")`
> `@GetMapping("/search")`
> `@PostMapping("/delete")`

## session、token

不需要单点登录，用session就足够