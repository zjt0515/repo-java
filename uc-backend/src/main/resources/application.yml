spring:
  application:
    name: uc-backend
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 123456
    url: jdbc:mysql://localhost:3306/uc
  # 失效时间
  session:
    timeout: 86400
server:
  port: 8080